package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Palette (Fresh & Minimalist Blue)
val PrimaryBlueLight = Color(0xFF2563EB)      // Vibrant royal blue
val SecondaryBlueLight = Color(0xFF64748B)    // Smooth slate gray
val TertiaryBlueLight = Color(0xFF0EA5E9)     // Ocean sky blue
val BackgroundLight = Color(0xFFF8FAFC)       // Minimal slate-50 light background
val SurfaceLight = Color(0xFFFFFFFF)          // Pure white panels
val SurfaceVariantLight = Color(0xFFE2E8F0)   // Light slate board borders (slate-200)
val OnPrimaryLight = Color(0xFFFFFFFF)
val OnSecondaryLight = Color(0xFFFFFFFF)
val OnBackgroundLight = Color(0xFF0F172A)      // Deep slate-900 text
val OnSurfaceLight = Color(0xFF0F172A)

// Dark Theme Palette (Clean Minimalism Deep Slate)
val PrimaryBlueDark = Color(0xFF3B82F6)       // Glowing digital blue (blue-500)
val SecondaryBlueDark = Color(0xFF94A3B8)     // Muted slate text/indicator (slate-400)
val TertiaryBlueDark = Color(0xFF38BDF8)      // Bright sky blue (sky-400)
val BackgroundDark = Color(0xFF0B0E14)        // Clean Minimalism signature background (#0B0E14)
val SurfaceDark = Color(0xFF0F172A)           // Solid slate-900 container
val SurfaceVariantDark = Color(0xFF1E293B)    // Slate-800 divider/outline boarders
val OnPrimaryDark = Color(0xFFFFFFFF)
val OnSecondaryDark = Color(0xFFF1F5F9)       // Soft white-shimmer (slate-100)
val OnBackgroundDark = Color(0xFFE2E8F0)      // Slate-200 readable text
val OnSurfaceDark = Color(0xFFF1F5F9)         // Slate-100 pure text
